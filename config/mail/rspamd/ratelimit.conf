rates {
  # Limit for all mail per recipient (rate 10 per minute)
  to = "10 / 1m";
  # Limit for all mail per one source ip (rate 15 per minute)
  to_ip = "15 / 1m";
  # Limit for all mail per one source ip and from address (rate 10 per minute)
  to_ip_from = "10 / 1m";
  # Limit for all bounce mail (rate 2 per hour)
  bounce_to = "2 / 1h";
  # Limit for bounce mail per one source ip (rate 1 per hour)
  bounce_to_ip = "1 / 1h";
  # Limit for all mail per authenticated user (rate 10 per minute)
  user = "10 / 1m";
}

whitelisted_rcpts = "postmaster,mailer-daemon";
max_rcpt = 5;
