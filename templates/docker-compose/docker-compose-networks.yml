  nginx:
    image: registry.gitlab.e.foundation:5000/e/infra/docker-nginx:1.15
    container_name: nginx
    restart: unless-stopped
    networks:
      - serverbase
    ports:
      - "80:8000"
      - "443:4430"
    volumes:
      - /mnt/docker/nginx/sites-enabled:/etc/nginx/conf.d/
      - /mnt/docker/nginx/params:/etc/nginx/params/
      - /mnt/docker/letsencrypt/certstore:/certs
      - /mnt/docker/nginx/passwds:/passwds
      - /mnt/docker/letsencrypt/acme-challenge:/etc/letsencrypt/acme-challenge

networks:
  serverbase:
    driver: 'bridge' 
